def insertion_sort(data, pointer=1):

    items_length = len(data)

    if pointer == items_length:
        return data

    sample = data[pointer]

    middle = int(items_length/2)
    while items_length > 1:
        if sample > data[middle-1] and sample < data[middle+1]
        ...
    return insertion_sort(data, pointer+1)


def binary_search(data, target):

    items_length = len(data)


    if :
        return